diff --git a/src/sif/pedestriancost.cc b/src/sif/pedestriancost.cc
--- a/src/sif/pedestriancost.cc
+++ b/src/sif/pedestriancost.cc
@@ -62,6 +62,7 @@
 constexpr float kDefaultDrivewayFactor = 5.0f;     // Avoid driveways
 constexpr float kDefaultUseFerry = 1.0f;
 constexpr float kDefaultUseLivingStreets = 0.6f; // Factor between 0 and 1
+constexpr float kDefaultPathFactor = 1.0f;       // Neutral value for highway=path
 
 // Maximum distance at the beginning or end of a multimodal route
 // that you are willing to travel for this mode.  In this case,
@@ -118,6 +119,7 @@
 constexpr ranged_default_t<uint32_t> kMaxHikingDifficultyRange{0, kDefaultMaxHikingDifficulty, 6};
 constexpr ranged_default_t<float> kModeFactorRange{kMinFactor, kModeFactor, kMaxFactor};
 constexpr ranged_default_t<float> kWalkwayFactorRange{kMinFactor, kDefaultWalkwayFactor, kMaxFactor};
+constexpr ranged_default_t<float> kPathFactorRange{kMinFactor, kDefaultPathFactor, kMaxFactor};
 constexpr ranged_default_t<float> kSideWalkFactorRange{kMinFactor, kDefaultSideWalkFactor,
                                                        kMaxFactor};
 constexpr ranged_default_t<float> kAlleyFactorRange{kMinFactor, kDefaultAlleyFactor, kMaxFactor};
@@ -462,6 +464,9 @@
       if (service_factor_ < 1.f) {
         factor *= service_factor_;
       }
+      if (path_factor_ < 1.f) {
+        factor *= path_factor_;
+      }
 
       return (speedfactor_ * factor * min_linear_cost_factor_);
     } else {
@@ -527,6 +532,7 @@
   float speed_;                    // Pedestrian speed.
   float speedfactor_;              // Speed factor for costing. Based on speed.
   float walkway_factor_;           // Factor for favoring walkways and paths.
+  float path_factor_;              // Factor for highway=path edges.
   float sidewalk_factor_;          // Factor for favoring sidewalks.
   float alley_factor_;             // Avoid alleys factor.
   float driveway_factor_;          // Avoid driveways factor.
@@ -639,6 +645,7 @@
 
   mode_factor_ = costing_options.mode_factor();
   walkway_factor_ = costing_options.walkway_factor();
+  path_factor_ = costing_options.path_factor();
   sidewalk_factor_ = costing_options.sidewalk_factor();
   alley_factor_ = costing_options.alley_factor();
   driveway_factor_ = costing_options.driveway_factor();
@@ -665,6 +672,7 @@
   use_factor_[static_cast<uint8_t>(Use::kTrack)] = track_factor_;
   use_factor_[static_cast<uint8_t>(Use::kLivingStreet)] = living_street_factor_;
   use_factor_[static_cast<uint8_t>(Use::kServiceRoad)] = service_factor_;
+  use_factor_[static_cast<uint8_t>(Use::kPath)] = path_factor_;
 }
 
 // Check if access is allowed on the specified edge. Disallow if no
@@ -911,6 +919,7 @@
                           max_hiking_difficulty, warnings);
   JSON_PBF_RANGED_DEFAULT(co, kModeFactorRange, json, "/mode_factor", mode_factor, warnings);
   JSON_PBF_RANGED_DEFAULT(co, kWalkwayFactorRange, json, "/walkway_factor", walkway_factor, warnings);
+  JSON_PBF_RANGED_DEFAULT(co, kPathFactorRange, json, "/path_factor", path_factor, warnings);
   JSON_PBF_RANGED_DEFAULT(co, kSideWalkFactorRange, json, "/sidewalk_factor", sidewalk_factor,
                           warnings);
   JSON_PBF_RANGED_DEFAULT(co, kAlleyFactorRange, json, "/alley_factor", alley_factor, warnings);
diff --git a/proto/descriptors/options.proto b/proto/descriptors/options.proto
--- a/proto/descriptors/options.proto
+++ b/proto/descriptors/options.proto
@@ -358,6 +358,9 @@
     oneof has_multimodal_start_end_max_distance {
       uint32 multimodal_start_end_max_distance = 96;
     }
+    oneof has_path_factor {
+      float path_factor = 97;
+    }
   }
 
   oneof has_options {
